#include <vector>
#include <iostream>
#include <fstream>

const double a = 1, b = 1.5, dx = 1e-3, dy = 1e-3, dt = 1e-5; // dimensions of the membrane and the spacial/time steps
const int maxTime = 1000, Nx = a/dx, Ny = b/dy; // number of steps in time and spacial directions

void iterationScheme(vector<vector<double>> u, vector<double> init){
    
}

using namespace std;

int main(){


    return 0;
}